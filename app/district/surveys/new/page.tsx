export const dynamic = "force-dynamic";

import CreateSurveyPage from "@/components/survey/Newsurvey";
import React from "react";

const page = () => {
  return (
    <div>
      <CreateSurveyPage />
    </div>
  );
};

export default page;
